import React from 'react';
import { Category } from '@/types/api';
import { categoryList } from '@/constants/words/categoryList';

interface Props {
  categories: Category[];
}

export default function Home(props: Props) {
  return <>aaaaaaaa</>;
}
